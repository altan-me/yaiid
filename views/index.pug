extends layout
block layout-content
  #id
    #wrapper.wrapper
      .hub
        span
          form#pingCheck(action="/ping", method="POST")
            input#url.url(
              type="text",
              name="url",
              value="",
              placeholder="example.com",
              required
            )
            input.submit(type="submit", value="Submit")
      #response.response
      #detail.detail
